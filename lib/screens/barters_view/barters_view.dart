
import 'package:flutter/material.dart';
class BartersView extends StatelessWidget {
  const BartersView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child:Text('Barters'),
    );
  }
}
